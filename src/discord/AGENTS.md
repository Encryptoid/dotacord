# Summary

## register_server.rs

This command does not perform the normal process of validating the request. This is because the server is likely not registered yet.

Exposes the `/register_server` command to register the current Discord server with Dotacord.

