# register_server cmd line arg

```shell
dotacord register_server <server_id> <server_name>
```

Add a new cmd line arg that will register the server as a dotacord server.

This will not interact with discord at all, and will be a quick aside.

